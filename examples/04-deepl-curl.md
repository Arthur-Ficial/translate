# 04 · DeepL HTTP API · curl

`/v2/translate`, `/v2/languages`, `/v2/usage`. Form-encoded and JSON bodies.

> Generated by `scripts/generate-examples.py` from a live `translate` install. Each output below is the real captured stdout — none of it is hand-written.

### GET /v2/languages -- DeepL language list

```sh
curl -sf http://127.0.0.1:50644/v2/languages | python3 -c 'import sys, json; d = json.load(sys.stdin); print(json.dumps(d[:3], indent=2))'
```

```
[
  {
    "language": "AR",
    "name": "Arabic"
  },
  {
    "language": "BG",
    "name": "Bulgarian"
  },
  {
    "language": "CS",
    "name": "Czech"
  }
]
```

exit code: `0`


### GET /v2/usage -- DeepL quota stub

```sh
curl -sf http://127.0.0.1:50644/v2/usage
```

```
{"character_count":0,"character_limit":1000000000}
```

exit code: `0`


### POST /v2/translate -- single text (form-encoded)

_skipped — de-en model not installed_


### POST /v2/translate -- multiple texts in one request

_skipped — de-en model not installed_


### POST /v2/translate -- JSON body with array text

_skipped — de-en model not installed_


### POST /v2/translate -- missing target_lang -> 400

```sh
curl -s -o /dev/null -w '%{http_code}' -X POST http://127.0.0.1:50644/v2/translate --data-urlencode 'text=Hallo'
```

```
400
```

exit code: `0`

