# 07 · LibreTranslate HTTP API · libretranslatepy

Drives `translate --serve` with the community `libretranslatepy` client.

> Generated by `scripts/generate-examples.py` from a live `translate` install. Each output below is the real captured stdout — none of it is hand-written.

### libretranslatepy LibreTranslateAPI.languages()

```python
from libretranslatepy import LibreTranslateAPI
api = LibreTranslateAPI("http://127.0.0.1:50855")
ls = api.languages()
print(f"got {len(ls)} languages; first two: {[l['code'] for l in ls[:2]]}")
```

```
got 33 languages; first two: ['ar', 'bg']
```

exit code: `0`


### libretranslatepy LibreTranslateAPI.detect()

```python
from libretranslatepy import LibreTranslateAPI
api = LibreTranslateAPI("http://127.0.0.1:50855")
d = api.detect("Das ist ein deutscher Satz mit genug Worten.")
print(d)
```

```
[{'confidence': 100, 'language': 'de'}]
```

exit code: `0`


### libretranslatepy LibreTranslateAPI.translate()

_skipped — de-en model not installed_

