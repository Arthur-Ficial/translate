# 08 · Google Translate v2 HTTP API · curl

`/language/translate/v2`. Form (single + repeated `q`) and JSON bodies.

> Generated by `scripts/generate-examples.py` from a live `translate` install. Each output below is the real captured stdout — none of it is hand-written.

### GET /language/translate/v2/languages

```sh
curl -sf http://127.0.0.1:50644/language/translate/v2/languages | python3 -c 'import sys, json; d = json.load(sys.stdin); print(json.dumps({"sample": d["data"]["languages"][:3]}, indent=2))'
```

```
{
  "sample": [
    {
      "language": "ar",
      "name": "Arabic"
    },
    {
      "language": "bg",
      "name": "Bulgarian"
    },
    {
      "language": "cs",
      "name": "Czech"
    }
  ]
}
```

exit code: `0`


### POST /language/translate/v2 -- single q (form)

_skipped — de-en model not installed_


### POST /language/translate/v2 -- repeated q params (batch)

_skipped — de-en model not installed_


### POST /language/translate/v2 -- JSON body

_skipped — de-en model not installed_

