# 10 · Cross-API consistency

The same prompt, sent through three different protocols, must return identical text — proving they share a single on-device engine.

> Generated by `scripts/generate-examples.py` from a live `translate` install. Each output below is the real captured stdout — none of it is hand-written.

### Same input through all three APIs returns identical output

_skipped — de-en model not installed_

